# 立项：AI Coding Skills v0.1

## 目标
给不同 AI 和不同项目复用的需求→定位→验证→交接工作流，减少无目标探索与重复上下文。

## 非目标
不替代工程判断、不强制每个小任务完整走流程、不自动调用付费模型、不自动部署或改账号。
不做全语言语义索引、向量检索平台或多 Agent 编排服务。

## 验收
- R1 技能：任务范围、权限、停止条件和渐进阅读明确。
- R2 模板：需求验收、代码入口、文件归属、回滚和交接可复制且不含业务信息。
- R3 索引：同名不覆盖、源码变化拒绝旧查询、失败不替换旧索引、无业务模块导入。
- R4 可移植：标准库与 Git，无硬编码机器路径；CI 覆盖 Linux/Windows。
- R5 质量：合成仓库测试；不通过真实模型验证工具本身。

## 里程碑
M1：交付技能、模板、Python 索引和离线测试。
M2：在至少两个不同项目记录首次补丁前读取量、重复读取次数、测试结果、回归数量，再决定是否扩展。
M3：有明确需求再增加其他语言解析器，不提前建设。

## 风险与回滚
索引只提供静态候选关系；动态调用用测试确认。删除本地生成索引即可回到 rg 导航。
项目规则合并必须 review；回滚独立维护提交，不回滚业务安全修复或他人工作。
