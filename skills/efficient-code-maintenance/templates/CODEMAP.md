# Code map

Map each active subsystem to its entry symbol, responsibilities and relevant test command.
Do not copy source code, credentials, generated line numbers or the whole file tree here.
Distinguish verified coverage from merely related tests. Document unsafe live acceptance commands.
For unsupported languages, record the existing navigation tool rather than inventing an index.
